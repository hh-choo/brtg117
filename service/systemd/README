1. log_rotate.service
  - /nfs/log/data.log 파일을 10K 단위로 압축하는 log_rorate.sh을 실행 시키는 서비스

2. log_rotate.timer
  - 10초 단위로 log_rorate.service를 실행 시키는 서비스

3. tcpdump.service
  - 백그라운드로 /nfs/log/data.log에 ens7의 패킷을 모두 저장시키는 서비스

4. tcp.service
  - tcp.sh을 실행 시킨 후 tcp-rm.sh 을 실행시키는 서비스
